package com.semweb.Controller;

public class SPARQLGenerateController {

    /*public void generateModel(String queryString, String inputStream){
        
        Dataset inputDataset = null;
        QuerySolution initialBindings = new QuerySolutionMap();
        Context context = new Context();
        
        SPARQLGenerateQuery query = (SPARQLGenerateQuery) QueryFactory.create(queryString, SPARQLGenerate.SYNTAX);
        
        RootPlan plan = PlanFactory.create(query);

        // one of:
        Model execGenerate = plan.execGenerate(inputDataset, initialBindings, context);
        String execTemplate(inputDataset, initialBindings, context);
        ResultSet execSelect(inputDataset, initialBindings, context);
        
        Model model = ModelFactory.createDefaultModel();
        RDFDataMgr.read(model, inputStream, Lang.TURTLE);
    */
}
